#include <stdio.h>

/**
 * add - it adds two numbers
 * @a: integer parameter being checked
 * @b: interger being added
 *
 * Return: Always 0 (Success)
*/
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
