#include <stdio.h>

/**
 * mul - multiplies two numbers
 * @a: first parameter being multiplied
 * @b: second parameter being multiplied
 *
 * Return: always 0 (Success)
 */
/*int main(void)*/
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}

